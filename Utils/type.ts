export interface ProductType {
  productcode: string;
  productname: string;
  Price: string;
  pathimg: string;
}

export interface UserProfile {
  FullName?: string;
  Email?: string;
  Photo?: string;
} 